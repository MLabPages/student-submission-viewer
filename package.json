{
  "name": "student-submission-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Word, PowerPoint and PDF submissions viewer with layout-preserving previews",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  }
}
